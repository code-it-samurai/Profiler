Generate search queries to find a {{ target_type }} named "{{ target_name }}" on the internet.

{% if initial_context %}Additional context: {{ initial_context }}{% endif %}

{% if known_facts %}
Known facts:
{% for key, value in known_facts.items() %}
- {{ key }}: {{ value }}
{% endfor %}
{% endif %}

{% if search_history %}
Already searched (DO NOT repeat these):
{% for q in search_history %}
- {{ q }}
{% endfor %}
{% endif %}

Generate 3-5 search queries. DuckDuckGo supports these operators:
1. Quotes for exact match: "{{ target_name }}"
2. site: for domain restriction: site:linkedin.com "{{ target_name }}"
3. Combining name with facts: "{{ target_name }}" Portland

Query patterns to use:
- "{{ target_name }}" (plain exact name search)
- Use site_filter for platform-specific searches (linkedin.com, facebook.com, twitter.com)
- Combine name with one known fact per query for precision
- Keep queries SIMPLE — no +, inurl:, OR operators

IMPORTANT:
- Set site_filter to null (not the string "null") when no domain restriction is needed.
- When using site_filter, put ONLY the person's name in the query field (the site: prefix is added automatically).
- Generate at least one plain name search with no site_filter.

Respond with ONLY this JSON:
{
    "queries": [
        {"query": "search string", "site_filter": null, "purpose": "why this query"}
    ]
}
