Generate search queries to find a {{ target_type }} named "{{ target_name }}" on the internet.
Use advanced search operators (Google dorks) for precision.

{% if initial_context %}Additional context: {{ initial_context }}{% endif %}

{% if known_facts %}
Known facts:
{% for key, value in known_facts.items() %}
- {{ key }}: {{ value }}
{% endfor %}
{% endif %}

{% if search_history %}
Already searched (DO NOT repeat these):
{% for q in search_history %}
- {{ q }}
{% endfor %}
{% endif %}

Generate 3-5 search queries using these techniques:
1. Use quotes for exact name match: "{{ target_name }}"
2. Use site: operator for platform-specific searches: site:linkedin.com inurl:"/in/" "{{ target_name }}"
3. Combine name with known facts using quotes: "{{ target_name }}" "Portland" (resume OR bio OR profile)
4. Use OR to broaden: "{{ target_name }}" (employer OR works OR employee)
5. Use inurl: for profile pages: site:facebook.com inurl:"people" OR inurl:"profile" "{{ target_name }}"

Important:
- Set site_filter to null (not the string "null") if the query already contains a site: operator or if no site filter is needed.
- Only use site_filter for simple domain restrictions. If using advanced operators, put everything in the query string.

Respond with ONLY this JSON:
{
    "queries": [
        {"query": "search string with dork operators", "site_filter": null, "purpose": "why this query"}
    ]
}
